/**
 * Note: This file is intentionally empty.
 * You can use it to test your skills at adding, removing, or modifying
 * attributes, classes, and inline styles on elements.
 */
if(document.querySelector(".siteheader").classList.contains("hellocolor")){
    document.querySelector(".siteheader").style.background = "blue";
}else{
    document.querySelector(".siteheader").style.background = "red";

}